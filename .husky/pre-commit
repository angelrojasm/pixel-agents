#!/bin/sh
set -e

# Gitleaks step removed for this internal fork (same decision as the previous
# fork's first commit, 176a378): keep lint-staged formatting only.
echo "🎨 Formatting staged files..."
npm exec -- lint-staged
